FROM docker
RUN apk add python3 openjdk17-jre git && \
    cd home && \
    git clone https://github.com/QueueInc/HAMLET.git && \
    cd HAMLET && \
    chmod 777 scripts/*
WORKDIR /home/HAMLET
CMD ["bash", "scripts/run_experiments.sh"]
